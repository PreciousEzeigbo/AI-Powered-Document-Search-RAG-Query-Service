# OPTION 1: Google Gemini (Recommended if using Gemini models + Gemini embeddings)
# Get your key at: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here
USE_GOOGLE=true


# MODEL CONFIGURATION (Gemini)
# Gemini embedding + LLM models
EMBEDDING_MODEL=text-embedding-004
LLM_MODEL=gemini-1.5-flash
PROVIDER=google   # used by your code to route correctly


# =============================================================================
# Alternate Providers (Commented Out)
# =============================================================================

# If using OpenRouter:
# EMBEDDING_MODEL=google/text-embedding-004
# LLM_MODEL=google/gemini-1.5-flash
# PROVIDER=openrouter

# If using OpenAI directly:
# EMBEDDING_MODEL=text-embedding-3-small
# LLM_MODEL=gpt-4o
# PROVIDER=openai

# If using Anthropic (requires OpenAI for embeddings):
# EMBEDDING_MODEL=text-embedding-3-small
# LLM_MODEL=claude-3-5-sonnet-20241022
# PROVIDER=anthropic


# Vector Store Configuration
VECTOR_STORE_TYPE=chromadb  # Options: chromadb, pinecone


# Pinecone Configuration
# =============================================================================
# PINECONE_API_KEY=your_pinecone_api_key
# PINECONE_ENVIRONMENT=us-west1-gcp

# Database Configuration
DATABASE_PATH=rag_documents.db

# Chunking Configuration
CHUNK_SIZE=500
CHUNK_OVERLAP=50

# Server Configuration
HOST=0.0.0.0
PORT=8000
